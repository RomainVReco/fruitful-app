import React, { useState, useEffect } from "react";

function ScrollToTopButton() {

  return (
    <div>
      <button type="submit">Scroll to Top</button>
      Coucou
    </div>
  );
}

export default ScrollToTopButton;